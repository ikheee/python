# Python은 변수가 어떤 데이터 타입인지 추측합니다.

# input 함수가 문자열을 리턴하기 때문에 할당된 변수는 문자열 값을 가지고 있습니다.
first_num = input('Enter first number ')
second_num = input('Enter second number ')

# first_num과 second_num은 문자열 변수이기 때문에 + 연산자는 first_name과 last_name을 연결(concatenating)합니다.
print(first_num + second_num)